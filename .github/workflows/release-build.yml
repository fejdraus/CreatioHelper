name: Build Release

permissions:
  contents: write

on:
  release:
    types: [published]

jobs:
  build:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        include:
          - os: windows-latest
            runtime: win-x64
          - os: ubuntu-latest
            runtime: linux-x64

    steps:
      - uses: actions/checkout@v3

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: "8.0.x"

      - name: Restore all projects for this RID
        run: dotnet restore CreatioHelper.sln --runtime ${{ matrix.runtime }}

      - name: Publish self-contained build on Windows
        if: runner.os == 'Windows'
        run: dotnet publish CreatioHelper/CreatioHelper.csproj --configuration Release --runtime ${{ matrix.runtime }} --self-contained true /p:PublishReadyToRun=true /p:PublishSingleFile=true /p:IncludeNativeLibrariesForSelfExtract=true /p:EnableCompressionInSingleFile=true /p:DebugType=None /p:DebugSymbols=false --output publish --no-restore
        shell: pwsh

      - name: Publish self-contained build on Linux
        if: runner.os == 'Linux'
        run: dotnet publish CreatioHelper/CreatioHelper.csproj --configuration Release --runtime ${{ matrix.runtime }} --self-contained true /p:PublishReadyToRun=true /p:PublishSingleFile=true /p:IncludeNativeLibrariesForSelfExtract=true /p:EnableCompressionInSingleFile=true /p:DebugType=None /p:DebugSymbols=false --output publish --no-restore
        shell: bash

      - name: Archive build on Windows
        if: runner.os == 'Windows'
        run: Compress-Archive -Path publish/* -DestinationPath CreatioHelper-${{ matrix.runtime }}.zip
        shell: pwsh

      - name: Archive build on Linux
        if: runner.os == 'Linux'
        run: |
          cd publish
          zip ../CreatioHelper-${{ matrix.runtime }}.zip *
        shell: bash

      - name: Upload ZIP to GitHub Release
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          upload_url: ${{ github.event.release.upload_url }}
          asset_path: CreatioHelper-${{ matrix.runtime }}.zip
          asset_name: CreatioHelper-${{ matrix.runtime }}.zip
          asset_content_type: application/zip
